<% if ((params[:controller] == 'contents') && (params[:action] == 'edit')) -%>
  <%= javascript_include_tag "http://assets.visitindy.com/ckeditor/ckeditor.js" %>
  <script>
    // This call can be placed at any point after the
    // <textarea>, or inside a <head><script> in a
    // window.onload event handler.

    // Replace the <textarea id="editor"> with an CKEditor
    // instance, using default configurations.
    CKEDITOR.replace( 'html', {
    	allowedContent: true,
    	customConfig: '/assets/base2-editor.js'
     });
    CKEDITOR.config.contentsCss = ['//cloud.typography.com/7545272/708384/css/fonts.css', 'https://assets.visitindy.com/web_article.css'];
    // HEY
  </script>
<% end -%>