DEBUG: < %= edit_array_select_tag("Select #{event_or_meeting}", 'meeting_id', @meetings.get_verified_collection((meeting_date.meeting_id rescue 0)), (meeting_date.meeting_id rescue 0), :include_blank => '') %>
